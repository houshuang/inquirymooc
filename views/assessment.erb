<html>
  <head><title>Choosing group</title></head>
  <body>
    <h1>Choosing Study Group</h1>
    <form action="/study-group" method="post">
      <p>Hi, please choose which study group you'd like to belong to.
      <select name='group'>
      <option value='red'>Red</option>
      <option value="blue">Blue</option>
      <input type='submit' value='Submit' />
      <% @tp.to_params.each do |key, value| %>
        <input type="hidden" name="launch_params[<%= key %>]" value="<%= value %>" />
      <% end%>
    </form>
  </body>
</html>